Quick Start
===========

Install Hydra Router and start using the reference *ping/pong* system:

.. code-block:: bash

    # Install the package
    pip install hydra-router

    # Start a pong server (in one terminal)
    hydra-pong-server

    # Send pings from a client (in another terminal)
    hydra-ping-client --count 5